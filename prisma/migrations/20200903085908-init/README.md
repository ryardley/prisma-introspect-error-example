# Migration `20200903085908-init`

This migration has been generated by Rudi Yardley at 9/3/2020, 6:59:08 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post" (
"id" text   NOT NULL ,
"title" text   NOT NULL ,
"content" text   ,
"published" boolean   NOT NULL DEFAULT false,
"authorId" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Tag" (
"id" text   NOT NULL ,
"label" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"email" text   NOT NULL ,
"name" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."_PostToTag" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE TABLE "public"."_Antitags" (
"A" text   NOT NULL ,
"B" text   NOT NULL 
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

CREATE UNIQUE INDEX "_PostToTag_AB_unique" ON "public"."_PostToTag"("A", "B")

CREATE INDEX "_PostToTag_B_index" ON "public"."_PostToTag"("B")

CREATE UNIQUE INDEX "_Antitags_AB_unique" ON "public"."_Antitags"("A", "B")

CREATE INDEX "_Antitags_B_index" ON "public"."_Antitags"("B")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."_PostToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_PostToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_Antitags" ADD FOREIGN KEY ("A")REFERENCES "public"."Post"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_Antitags" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200903085908-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,33 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Post {
+  id        String  @id @default(dbgenerated())
+  title     String
+  content   String?
+  published Boolean @default(false)
+  author    User?   @relation(fields: [authorId], references: [id])
+  authorId  String?
+  tags      Tag[]
+  antitags  Tag[]   @relation("Antitags")
+}
+
+model Tag {
+  id        String @id @default(dbgenerated())
+  label     String
+  posts     Post[]
+  antiposts Post[] @relation("Antitags")
+}
+
+model User {
+  id    String  @id @default(dbgenerated())
+  email String  @unique
+  name  String?
+  posts Post[]
+}
```


